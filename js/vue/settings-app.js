const { createApp } = Vue;

createApp({
  data() {
    return {
      // Settings page is mostly static, but will add reactive features later
    };
  },
  mounted() {
    // Settings page is static for now
  },
  methods: {
    // Can add methods for settings management later
  }
}).mount('#settings-app');
